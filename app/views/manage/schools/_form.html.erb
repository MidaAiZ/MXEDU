<%= form_with(model: manage_school, local: true) do |form| %>
  <% if manage_school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manage_school.errors.count, "error") %> prohibited this manage_school from being saved:</h2>

      <ul>
      <% manage_school.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, id: :manage_school_city %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :manage_school_name %>
  </div>

  <div class="field">
    <%= form.label :cate %>
    <%= form.text_field :cate, id: :manage_school_cate %>
  </div>

  <div class="field">
    <%= form.label :sign %>
    <%= form.text_field :sign, id: :manage_school_sign %>
  </div>

  <div class="field">
    <%= form.label :info %>
    <%= form.text_field :info, id: :manage_school_info %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
